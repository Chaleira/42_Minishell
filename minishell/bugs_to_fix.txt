ls |
pipe> (
pipe> asd
minihell: seg fault
bash: valid input

NOTE: When u open a pipe with '(' you need to wait for the program to receive a ')'

ls | << asd
pipe> asd
minihell: syntax error cause '<<' is next to '|'
bash: valid input

NOTE: Need to createa function to find a pair for the "()", because if one is opened but its not closed
	  minihell needs to syntax error